{#
/**
 * @file
 * Theme override to display the front page.
 */
#}
<div class="university-homepage">
  {# Hero Section #}
  <section class="hero-section bg-primary text-white py-5">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-lg-6">
          <h1 class="display-4 fw-bold mb-4">{{ 'Transform Your Future'|t }}</h1>
          <p class="lead mb-4">{{ 'Discover world-class education and research opportunities at our prestigious university.'|t }}</p>
          <div class="hero-buttons">
            <a href="/programs" class="btn btn-light btn-lg me-3">{{ 'Explore Programs'|t }}</a>
            <a href="/admissions" class="btn btn-outline-light btn-lg">{{ 'Apply Now'|t }}</a>
          </div>
        </div>
        <div class="col-lg-6">
          <div class="hero-image">
            <img src="/themes/contrib/dxpr_theme/images/dxpr-logo-white.svg" alt="University Campus" class="img-fluid">
          </div>
        </div>
      </div>
    </div>
  </section>

  {# Stats Section #}
  <section class="stats-section py-5 bg-light">
    <div class="container">
      <div class="row text-center">
        <div class="col-md-3 mb-4">
          <div class="stat-item">
            <h3 class="display-6 text-primary fw-bold">15,000+</h3>
            <p class="text-muted">{{ 'Students'|t }}</p>
          </div>
        </div>
        <div class="col-md-3 mb-4">
          <div class="stat-item">
            <h3 class="display-6 text-primary fw-bold">200+</h3>
            <p class="text-muted">{{ 'Programs'|t }}</p>
          </div>
        </div>
        <div class="col-md-3 mb-4">
          <div class="stat-item">
            <h3 class="display-6 text-primary fw-bold">95%</h3>
            <p class="text-muted">{{ 'Graduation Rate'|t }}</p>
          </div>
        </div>
        <div class="col-md-3 mb-4">
          <div class="stat-item">
            <h3 class="display-6 text-primary fw-bold">150+</h3>
            <p class="text-muted">{{ 'Countries'|t }}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  {# Programs Section #}
  <section class="programs-section py-5">
    <div class="container">
      <div class="row">
        <div class="col-12 text-center mb-5">
          <h2 class="display-5 fw-bold">{{ 'Academic Programs'|t }}</h2>
          <p class="lead text-muted">{{ 'Choose from our diverse range of undergraduate and graduate programs'|t }}</p>
        </div>
      </div>
      <div class="row">
        {{ drupal_block('programsblock') }}
      </div>
      <div class="row">
        <div class="col-12 text-center mt-4">
          <a href="/programs" class="btn btn-primary btn-lg">{{ 'View All Programs'|t }}</a>
        </div>
      </div>
    </div>
  </section>

  {# News Section #}
  <section class="news-section py-5 bg-light">
    <div class="container">
      <div class="row">
        <div class="col-12 text-center mb-5">
          <h2 class="display-5 fw-bold">{{ 'Latest News'|t }}</h2>
          <p class="lead text-muted">{{ 'Stay updated with campus news and events'|t }}</p>
        </div>
      </div>
      <div class="row">
        {{ drupal_block('newsblock') }}
      </div>
    </div>
  </section>

  {# CTA Section #}
  <section class="cta-section py-5 bg-primary text-white">
    <div class="container">
      <div class="row text-center">
        <div class="col-lg-8 mx-auto">
          <h2 class="display-5 fw-bold mb-4">{{ 'Ready to Start Your Journey?'|t }}</h2>
          <p class="lead mb-4">{{ 'Join thousands of students who have transformed their lives through education at our university.'|t }}</p>
          <a href="/admissions" class="btn btn-light btn-lg me-3">{{ 'Apply Today'|t }}</a>
          <a href="/contact" class="btn btn-outline-light btn-lg">{{ 'Contact Us'|t }}</a>
        </div>
      </div>
    </div>
  </section>
</div>
